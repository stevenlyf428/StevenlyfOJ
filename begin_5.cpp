#include <stdio.h>
int main(void)
{
    long long x;
    // scanf("%I64d", &x);
    // printf("%I64d", (x + 1) * x / 2);
    scanf("%lld", &x);
    printf("%lld", (x + 1) * x / 2);
    return 0;
}
